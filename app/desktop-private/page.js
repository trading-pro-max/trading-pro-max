export default function DesktopPrivatePage() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "#020617", color: "white", fontFamily: "Arial, sans-serif" }}>
      <div style={{ textAlign: "center" }}>
        <div style={{ fontSize: 36, fontWeight: 900 }}>TPM Desktop Private</div>
        <div style={{ marginTop: 12, color: "#94a3b8" }}>Private desktop module is now connected to the new core.</div>
      </div>
    </main>
  );
}
